<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home"
    tools:ignore="UnusedNavigation">
    <fragment
        android:id="@+id/nav_home"
        android:name="com.android.ecommercex.fragment.Home"
        android:label="@string/menu_home"
        tools:layout="@layout/frag_home">
    </fragment>

    <fragment
        android:id="@+id/nav_cart"
        android:name="com.android.ecommercex.fragment.User"
        android:label="@string/menu_gallery"
        tools:layout="@layout/frag_cart" />

    <fragment
        android:id="@+id/nav_user"
        android:name="com.android.ecommercex.fragment.Login"
        android:label="@string/menu_slideshow"
        tools:layout="@layout/frag_login" />
</navigation>